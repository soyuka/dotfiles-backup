# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $alt Mod1
set $mod Mod4

set_from_resource $foreground foreground #000000
set_from_resource $black      black      #000000
set_from_resource $lightblack lightblack #000000
set_from_resource $grey       grey       #000000
set_from_resource $lightgrey  lightgrey  #000000
set_from_resource $darkwhite  darkwhite  #000000
set_from_resource $white      white      #000000
set_from_resource $red        red        #000000
set_from_resource $orange     orange     #000000
set_from_resource $yellow     yellow     #000000
set_from_resource $green      green      #000000
set_from_resource $cyan       cyan       #000000
set_from_resource $blue       blue       #000000
set_from_resource $magenta    magenta    #000000
set_from_resource $brown      brown      #000000
set $transparent #00000000

font pango:Source Code Pro 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
focus_follows_mouse yes

# start a terminal
bindsym $mod+Return exec alacritty -e ~/.config/i3/tmux.sh
bindsym $mod+Shift+Return exec alacritty

# kill focused window
bindsym $mod+Shift+q kill

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
# bindsym $mod+Shift+Left move left
# bindsym $mod+Shift+Down move down
# bindsym $mod+Shift+Up move up
# bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+Shift+backslash split h

# split in vertical orientation
bindsym $mod+Shift+minus split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle
bindsym $mod+space exec rofi -show run
bindsym $mod+Tab exec rofi -show window

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

set $term ""
set $web ""
set $data ""
set $audio ""

# switch to workspace
bindsym $mod+1 workspace $term
bindsym $mod+2 workspace $web
bindsym $mod+3 workspace $data
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace $audio
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10
bindsym $mod+Left workspace next
bindsym $mod+Right workspace prev

workspace $term output DVI-I-1
workspace $audio output DVI-D-1
workspace $web output DVI-D-0

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $term
bindsym $mod+Shift+2 move container to workspace $web
bindsym $mod+Shift+3 move container to workspace $data
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace $audio
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Sreen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 3 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 3 # decrease screen brightness
bindsym XF86AudioRaiseVolume exec pactl set-sink-volume $(pacmd list-sinks | grep "* index" | awk '{print $3}') +3%
bindsym XF86AudioLowerVolume exec  pactl set-sink-volume $(pacmd list-sinks | grep "* index" | awk '{print $3}') -3%
bindsym XF86AudioMute exec pactl set-sink-mute $(pacmd list-sinks | grep "* index" | awk '{print $3}') toggle
bindsym XF86AudioMicMute exec pactl set-source-mute $(pacmd list-sources | grep "* index" | awk '{print $3}') toggle
bindsym XF86Display exec ~/.config/i3/kbdbacklight.sh
bindsym XF86AudioPlay exec ~/.config/i3/sp.sh play
bindsym XF86AudioNext exec ~/.config/i3/sp.sh next
bindsym XF86AudioPrev exec ~/.config/i3/sp.sh prev
bindsym $mod+Shift+Right exec ~/.config/i3/sp.sh next
bindsym $mod+Shift+Left exec ~/.config/i3/sp.sh prev
bindsym $mod+Shift+P exec ~/.config/i3/sp.sh play

# Startup
exec --no-startup-id compton -b
exec --no-startup-id feh --bg-fill ~/Pictures/Backgrounds/rocket-league.png
exec --no-startup-id tmux -u -2 start-server
exec --no-startup-id ~/.config/i3/init.sh
# exec --no-startup-id yabar
# exec --no-startup-id xautolock -locker "i3lock -i ~/Pictures/Backgrounds/wallhaven-489193.png" -detectsleep

bindsym $mod+Shift+x exec ~/.config/i3/lockandsuspend.sh
bindsym XF86LaunchA exec scrot -s -e 'mv $f ~/Pictures/Screenshots'
bindsym XF86Explorer exec scrot -e 'mv $f ~/Pictures/Screenshots'

hide_edge_borders both
new_window pixel 0
gaps inner 10

bar {
    id bar-top
    height 27
    i3bar_command i3bar
    status_command i3blocks -c ~/.config/i3blocks/top.conf
    position top
    tray_output primary
    font pango:Source Code Pro 9
    # strip_workspace_numbers yes
    colors {
        statusline         $foreground
        background         $lightblack
        separator          $transparent

#                          BORDER       BACKGROUND   TEXT
        focused_workspace  $lightblack $lightblack $lightgrey
        inactive_workspace $lightblack $lightblack $lightgrey
        active_workspace   $lightblack $lightblack $lightgrey
        urgent_workspace   $red     $red     $transparent
        # binding_mode       $darkred     $darkred     $transparent
    }
}

bar {
    id bar-bottom
    height 27
    i3bar_command i3bar
    status_command i3blocks -c ~/.config/i3blocks/bottom.conf
    position bottom
    font pango:Adobe Source Code Pro 9
    workspace_buttons no
    colors {
        statusline         $foreground
        background         $lightblack
        separator          $transparent
    }
}

# pacmd list-sinks | grep -i 'volume: ' | head -n 1 | awk '{print $5}'


#                       BORDER      BACKGROUND  TEXT        INDICATOR   CHILD_BORDER
client.focused          $lightblack      $black      $white  $grey  $grey
client.unfocused        $grey      $lightblack      $white      $lightgrey  $lightgrey
client.focused_inactive $grey      $lightblack      $white      $lightgrey  $lightgrey
client.urgent           $red    $red    $black      $dred    $red
client.background       $black
